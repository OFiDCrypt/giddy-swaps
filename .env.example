# 🔐 Wallet
KEYPAIR_PATH=/path/to/your/mint-authority.json
# Absolute path to your Solana keypair file (e.g., /mnt/c/Users/YourName/mint-authority.json)

# 🪙 Token Mints
USDC_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
GIDDY_MINT=8kQzvMELBQGSiFmrXqLuDSpYVLKkNoXE4bUQCC14wj3Z
# USDC and GIDDY token mint addresses (mainnet)

# 🌐 RPC Configuration
RPC_URL=https://api.mainnet-beta.solana.com
RPC_RETRIES=5
RPC_TIMEOUT=30000
# Public RPCs may rate-limit or timeout. For production, consider a paid RPC (e.g., Helius)

# 🔁 Swap Parameters
SWAP_AMOUNT=100000
SLIPPAGE_BPS=200
MIN_OUT_AMOUNT=0
SWAP_INTERVAL=300
INITIAL_AMOUNT=10
MIN_SWAP_AMOUNT=0.01
MAX_BUY_USDC=10
INITIAL_DIRECTION=forward
# SWAP_AMOUNT: Raw units (e.g., 0.1 USDC = 100000)
# SLIPPAGE_BPS: 200 = 2%
# SWAP_INTERVAL: Seconds between swaps (e.g., 300 = 5 min)
# INITIAL_AMOUNT: Starting USDC amount (human-readable)
# MIN_SWAP_AMOUNT: Threshold to stop swapping
# MAX_BUY_USDC: Max USDC to swap on buy
# INITIAL_DIRECTION: 'forward' or 'reverse'

# 🛠️ DLMM Fallback (Optional)
USE_DLMM_FALLBACK=false
# Set to 'true' to enable DLMM fallback if Jupiter fails

# 📡 Telegram Alerts
TELEGRAM_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
# Get token from @BotFather, and chat ID from Telegram API or bot logs
